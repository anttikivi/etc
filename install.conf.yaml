- defaults:
    link:
      create: true
      relink: true
    create:
      mode: 0755

- clean: ["~", "~/.config"]

- link:
    ~/.aerospace.toml:
    ~/.config/nvim: nvim
    ~/.zprofile:
    ~/.zsh:
    ~/.zshenv:
    ~/.zshrc:
