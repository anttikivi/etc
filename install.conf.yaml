- defaults:
    link:
      create: true
      relink: true
    create:
      mode: 0755

- clean: ["~", "~/.config"]

- link:
    ~/.aerospace.toml:
    ~/.config/nvim: nvim
    ~/.editorconfig:
    ~/.local/bin/:
      glob: true
      path: bin/*
    ~/.tmux.conf:
    ~/.zprofile:
    ~/.zsh:
    ~/.zshenv:
    ~/.zshrc:
