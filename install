#!/usr/bin/env bash

set -e

readonly ESC="\033"
readonly ESC_RESET="${ESC}[0m"
readonly ESC_GREEN="${ESC}[32m"
readonly ESC_BLUE="${ESC}[34m"

if [ "${DOTFILES_UPDATE}" = 1 ]; then
  echo "Updates are set to true"
fi

CONFIG="install.conf.yaml"
DOTBOT_DIR="dotbot"

DOTBOT_BIN="bin/dotbot"
BASEDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

cd "${BASEDIR}"
git -C "${DOTBOT_DIR}" submodule sync --quiet --recursive
git submodule update --init --recursive "${DOTBOT_DIR}"

"${BASEDIR}/${DOTBOT_DIR}/${DOTBOT_BIN}" -d "${BASEDIR}" -c "${CONFIG}" "${@}"

printf "\n"

echo -e "${ESC_BLUE}Starting the kitty tasks${ESC_RESET}"

./scripts/install_kitty.sh

echo -e "${ESC_GREEN}==> Kitty tasks run${ESC_RESET}"
